# countdown

HTML com contagem regressiva entre duas datas.

## Rodar localmente

```bash
npm start
```

Abre em `http://localhost:3000`.

## Deploy no Railway

- O Railway vai usar `npm start` automaticamente.
- O servidor escuta `process.env.PORT` em `0.0.0.0`.
- Arquivo principal: `index.html`.
